"""
Application layer.

Use cases and orchestration live here. This layer depends on `domain/` and on
abstract ports defined in `ports.py`; it never imports from `infrastructure/`
or `api/` directly.
"""
