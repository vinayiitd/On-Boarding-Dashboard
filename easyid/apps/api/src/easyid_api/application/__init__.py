"""
Application layer.

Use cases and orchestration live here. This layer depends on the shared
domain package (`easyid_domain` in `packages/domain`) and on abstract
ports defined in `ports.py`; it never imports from `infrastructure/` or
`api/` directly.
"""
